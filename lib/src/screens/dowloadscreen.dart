import 'package:flutter/material.dart';

class DowloadScreen extends StatelessWidget {
  const DowloadScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container(color: Colors.yellowAccent);
}
